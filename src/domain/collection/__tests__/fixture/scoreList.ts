import * as fixtureValue from '../../../value/__tests__/fixture/index'
import ScoreList from '../../scoreList'

const scores = [fixtureValue.score]

const scoreList = new ScoreList(scores)

export default scoreList
