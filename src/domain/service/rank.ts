import Player from "../entities/player"
import PlayedDateRange from "../value/dateRange"

class Rank {
  private players: Player[]
  constructor(players: Player[]) {
    this.players = players
  }

  getPlayerRank(dateRange: PlayedDateRange) {

  }
}